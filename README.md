
  # Define User Roles & Flows

  This is a code bundle for Define User Roles & Flows. The original project is available at https://www.figma.com/design/agKaC3ax0gjUnl2kJ7heHA/Define-User-Roles---Flows.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  